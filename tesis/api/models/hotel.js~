var mongoose = require('mongoose');

module.exports = function(app, mongoose) {

	mongoose.model('Hotel', {
		destination: String,		
		name: String,
		stars: Number,
		services : [String],
		price: Number});
};
